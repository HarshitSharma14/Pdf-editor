# dependencies
/node_modules

# production
/.next
/out

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env

# vercel
.vercel 


# now listen there is one small issue. the cursor that we are using is the shape of a plus sign but has the actual pointer at the bottom and not the centre, so its causing confusion and also feeling like its going a bit over the top and leaving some area on the bottom of the pdf, can we do something about it? 